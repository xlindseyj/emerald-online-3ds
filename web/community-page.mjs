import fs from 'node:fs';
import path from 'node:path';

export const CURRENT_RELEASE = JSON.parse(fs.readFileSync(path.resolve(import.meta.dirname, '..', 'package.json'), 'utf8')).version;
export const communityPage = `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<link rel="icon" type="image/png" href="/favicon.png"><title>Community · Emerald Online 3DS</title>
<style>
:root{color-scheme:dark;font-family:ui-rounded,system-ui,sans-serif;background:#061b16;color:#edfff7}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 10% 0,#176348,#061b16 48%);min-height:100vh}a{color:#7ce6b4}button,input,textarea,select{font:inherit}button,.button{border:0;border-radius:10px;background:#58d69a;color:#052017;font-weight:800;padding:10px 14px;cursor:pointer}.secondary{background:#173c31;color:#cffff0;border:1px solid #64c99a55}.danger{background:#6c3037;color:#fff}.shell{width:min(1160px,100%);margin:auto;padding:20px}.topbar{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:12px 0 22px}.brand{display:flex;align-items:center;gap:12px;text-decoration:none;color:#effff8;font-size:1.1rem;font-weight:900}.brand img{width:48px;border-radius:22%}.session{display:flex;gap:10px;align-items:center;color:#b8ddcc}.layout{display:grid;grid-template-columns:245px minmax(0,1fr);gap:20px}.panel{background:#09251fee;border:1px solid #65d6a244;border-radius:18px;padding:20px;box-shadow:0 18px 60px #0005}.sidebar{align-self:start;position:sticky;top:18px}.sidebar h2{font-size:.82rem;letter-spacing:.1em;text-transform:uppercase;color:#78d8ab}.category{display:block;width:100%;text-align:left;margin:5px 0;background:transparent;color:#d9f8e9;padding:9px;border-radius:8px;border:0}.category:hover,.category.active{background:#174b39}.category.locked{color:#77998a}.toolbar{display:flex;flex-wrap:wrap;gap:10px;margin-bottom:18px}.toolbar input{flex:1;min-width:180px}.field,input,textarea,select{width:100%;background:#061b16;color:#effff8;border:1px solid #4e9f7a77;border-radius:9px;padding:10px}textarea{min-height:130px;resize:vertical}.topic{display:grid;grid-template-columns:1fr auto;gap:10px;padding:15px 2px;border-bottom:1px solid #40775e55;cursor:pointer}.topic:hover h3{color:#79e4b1}.topic h3{margin:0 0 6px;font-size:1rem}.meta,.muted{color:#92bbaa;font-size:.84rem}.badge{display:inline-block;border-radius:999px;background:#174b39;color:#a8f0cd;padding:3px 8px;font-size:.73rem;margin-right:5px}.badge.critical,.badge.high{background:#662f38;color:#ffd7dc}.body{line-height:1.6}.body pre{overflow:auto;background:#04120f;padding:14px;border-radius:10px}.body code{color:#a1edc9}.post{padding:18px 0;border-top:1px solid #40775e55}.actions{display:flex;flex-wrap:wrap;gap:8px;margin:16px 0}.hidden{display:none!important}.empty{text-align:center;padding:50px 10px;color:#93baa9}.pairbox{background:#102f27;border:1px solid #6addaa66;border-radius:14px;padding:16px;margin-bottom:18px}.paircode{font:900 2rem ui-monospace,monospace;letter-spacing:.08em;color:#ffdb8b;margin:10px 0}.notice{padding:11px;border-radius:9px;background:#18392f;margin:10px 0}.error{background:#5c2930;color:#ffdce0}.composer{display:grid;gap:12px}.twocol{display:grid;grid-template-columns:1fr 1fr;gap:10px}.site-footer{margin-top:28px;padding:22px 2px;color:#91bbaa;border-top:1px solid #65d6a244;font-size:.82rem;line-height:1.5}@media(max-width:760px){.layout{grid-template-columns:1fr}.sidebar{position:static}.topbar{align-items:flex-start}.twocol{grid-template-columns:1fr}.shell{padding:12px}.panel{padding:16px}}
.scoretable{width:100%;border-collapse:collapse}.scoretable th,.scoretable td{text-align:left;padding:11px;border-bottom:1px solid #40775e55}.scoretable th{color:#8ce4b9}.badge.official{background:#d4a72c;color:#142219;font-weight:900}.body h2,.body h3{margin-top:1.4em;color:#c9ffe4}.body ul{padding-left:1.35rem}.body li{margin:.35rem 0}.body img{display:block;width:min(100%,680px);max-height:440px;object-fit:contain;margin:18px auto;border:1px solid #65d6a244;border-radius:14px;background:#04140f}.body hr{border:0;border-top:1px solid #65d6a244;margin:24px 0}
</style></head><body><div class="shell"><header class="topbar"><a class="brand" href="/"><img src="/logo.png" alt=""><span>Emerald Online 3DS<br><small class="muted">Community beta</small></span></a><div id="session" class="session">Loading…</div></header>
<div id="pairbox" class="pairbox hidden"><strong>Pair this browser with your 3DS</strong><p>Keep the game online, tap your trainer profile on the bottom screen, and enter this temporary code. Pairing expires after five minutes.</p><div id="paircode" class="paircode">—</div><p id="pairmessage" class="muted"></p><button id="cancelpair" class="secondary">Cancel</button></div>
<div class="layout"><aside class="panel sidebar"><h2>Player</h2><button id="profilebutton" class="category">My profile &amp; privacy</button><button id="leaderboardbutton" class="category">Leaderboards</button><a class="category" href="/status">● Live service status</a><h2>Forum boards</h2><button class="category active" data-category="">All public topics</button><div id="categories"></div><p class="muted">Profiles and leaderboards require a paired 3DS. Help, releases, service status, and confirmed defects remain public.</p><details><summary>Pairing or recovery help</summary><p class="muted">Pairing is never required to install, check service status, or read confirmed defects. Keep the game online before entering a code. If an identity is lost, use its one-time recovery code from the game; never post that code or <code>identity.cfg</code>.</p></details></aside>
<main class="panel"><div id="notice" class="notice hidden"></div><section id="listing"><div class="toolbar"><input id="search" maxlength="120" placeholder="Search topics"><button id="searchbutton" class="secondary">Search</button><button id="newtopic">New topic</button></div><div id="topics"></div><div class="actions"><button id="previous" class="secondary">Previous</button><span id="page" class="muted"></span><button id="next" class="secondary">Next</button></div></section>
<section id="topicview" class="hidden"><button id="back" class="secondary">← Topics</button><div id="topic"></div></section>
<section id="profileview" class="hidden"><button class="secondary returntopics">← Topics</button><h1>My player profile</h1><div id="profile"></div></section>
<section id="leaderboardview" class="hidden"><button class="secondary returntopics">← Topics</button><h1>Leaderboards</h1><p class="muted">Save-derived scores are Community-submitted. Suspicious changes are excluded as Under review, not punished.</p><div class="toolbar"><select id="boardselect"><option value="pokedex-caught">Pokédex caught</option><option value="pokedex-seen">Pokédex seen</option><option value="badges">Badges</option><option value="frontier-streak">Battle Frontier streak</option><option value="online-battles">Completed online battles</option><option value="online-trades">Completed online trades</option><option value="beta-compatibility">Beta compatibility contributions</option></select><input id="variant" maxlength="80" placeholder="Frontier: tower:singles:50"><input id="releaseversion" value="${CURRENT_RELEASE}" maxlength="40"><button id="loadboard">Load</button></div><div id="leaderboard"></div></section>
<section id="composer" class="hidden"><button id="cancelcompose" class="secondary">← Cancel</button><h2>Create a topic</h2><form id="topicform" class="composer"><label>Board<select id="topiccategory" required></select></label><label>Title<input id="topictitle" required minlength="3" maxlength="120"></label><label>Post<textarea id="topicbody" required maxlength="10000" placeholder="Markdown and fenced code blocks are supported."></textarea></label>
<fieldset id="defectfields" class="composer hidden"><legend>Structured defect details</legend><div class="twocol"><label>Severity<select id="severity"><option>low</option><option selected>medium</option><option>high</option><option>critical</option></select></label><label>Runtime version<input id="runtimeversion" value="${CURRENT_RELEASE}" maxlength="40"></label><label>Console<select id="consolemodel"><option value="old-3ds">Old 3DS</option><option value="old-3ds-xl">Old 3DS XL</option><option value="new-3ds">New 3DS</option><option value="new-3ds-xl">New 3DS XL</option><option value="new-2ds-xl">New 2DS XL</option><option value="emulator">Emulator</option></select></label><label>Install<select id="installmethod"><option value="cia">CIA</option><option value="3dsx">3DSX</option></select></label><label>Transport<select id="transport"><option value="wss">Production WSS</option><option value="tcp">LAN TCP</option></select></label><label>Artifact SHA-256<input id="artifacthash" pattern="[a-f0-9]{64}" maxlength="64" required></label></div><label>Reproduction steps<textarea id="reproduction" maxlength="10000"></textarea></label><label>Expected behavior<textarea id="expected" maxlength="5000"></textarea></label><label>Actual behavior<textarea id="actual" maxlength="5000"></textarea></label><label>Sanitized diagnostics<textarea id="diagnostics" maxlength="10000" placeholder="Remove identity tokens, recovery codes, ROM/save data, and personal information."></textarea></label></fieldset><button type="submit">Publish topic</button></form></section></main></div>
<footer class="site-footer">Unofficial fan-made homebrew beta. Not affiliated with Nintendo, The Pokémon Company, or Game Freak. No ROMs, game files, keys, copyrighted artwork, or copyrighted audio are provided. Back up saves before testing and never post identity credentials or recovery codes.</footer></div><script src="/community.js" defer></script></body></html>`;

export const communityScript = `(() => {
'use strict';
const currentRelease='${CURRENT_RELEASE}';
const state={session:null,categories:[],category:'',query:'',page:1,current:null,pairing:null,pairTimer:null,leaderboardPage:1};
const $=id=>document.getElementById(id);
function notice(message,error=false){const el=$('notice');el.textContent=message;el.className='notice'+(error?' error':'');el.classList.toggle('hidden',!message)}
async function api(path,options={}){const headers={...(options.headers||{})};if(options.body&&!headers['content-type'])headers['content-type']='application/json';if(state.session?.csrfToken&&options.method&&options.method!=='GET')headers['x-csrf-token']=state.session.csrfToken;const response=await fetch(path,{...options,headers});const data=await response.json();if(!response.ok){const error=new Error(data.error||'request_failed');error.status=response.status;throw error}return data}
function date(value){return new Date(value).toLocaleString()}
function show(name){for(const id of ['listing','topicview','composer','profileview','leaderboardview'])$(id).classList.toggle('hidden',id!==name)}
async function loadSession(){const data=await api('/api/community/session');state.session=data.paired?data:null;renderSession()}
function renderSession(){const el=$('session');el.replaceChildren();if(state.session){const label=document.createElement('span');label.textContent='ID '+state.session.fingerprint;el.append(label);if(state.session.moderator)el.append(button('Moderation',openModeration));const out=document.createElement('button');out.className='secondary';out.textContent='Sign out';out.onclick=logout;el.append(out);if(state.session.sanctions?.length)notice('Your forum access is restricted: '+state.session.sanctions.map(x=>x.kind).join(', '),true)}else{const pair=document.createElement('button');pair.textContent='Pair 3DS';pair.onclick=startPairing;el.append(pair)}}
function requirePairing(){if(state.session)return true;notice('Pair your authenticated 3DS to access private profiles and leaderboards.',true);startPairing();return false}
function table(headers,rows){const table=document.createElement('table');table.className='scoretable';const head=document.createElement('thead'),tr=document.createElement('tr');for(const value of headers){const th=document.createElement('th');th.textContent=value;tr.append(th)}head.append(tr);const body=document.createElement('tbody');for(const row of rows){const line=document.createElement('tr');for(const value of row){const td=document.createElement('td');td.textContent=value;line.append(td)}body.append(line)}table.append(head,body);return table}
async function openProfile(){if(!requirePairing())return;try{const data=await api('/api/community/profile'),host=$('profile');host.replaceChildren();const status=document.createElement('div');status.className='pairbox';status.textContent=data.stats.enabled?'Leaderboard uploads are enabled on at least one paired device.':'No device currently has save-derived stat uploads enabled.';host.append(status);if(data.stats.scores.length)host.append(table(['Release','Board','Variant','Score','Integrity'],data.stats.scores.map(row=>[row.release_version||row.release,row.board,row.variant||'—',String(row.score),row.integrity_label+(row.review_status==='under-review'?' · Under review':'')])));else{const empty=document.createElement('p');empty.className='empty';empty.textContent='No uploaded statistics. Enable individual fields on the 3DS Stats screen or contribute a compatibility result.';host.append(empty)}const warning=document.createElement('p');warning.className='muted';warning.textContent='The server never receives Trainer ID, Pokémon, party, moves, inventory, save files, or ROM content.';const actions=document.createElement('div');actions.className='actions';actions.append(button('Submit compatibility result',submitCompatibility),button('Delete all uploaded statistics',deleteAllStats,'danger'));host.append(warning,actions);show('profileview')}catch(error){notice(error.message,true)}}
async function deleteAllStats(){if(!confirm('Permanently delete all current and historical leaderboard statistics for this identity?'))return;const phrase=prompt('Type DELETE ALL STATS to confirm');if(phrase!=='DELETE ALL STATS')return;try{await api('/api/community/stats',{method:'DELETE',body:JSON.stringify({confirm:phrase})});notice('All uploaded statistics were deleted. Device uploads must also be disabled on each 3DS to prevent a later re-upload.');await openProfile()}catch(error){notice(error.message,true)}}
async function submitCompatibility(){const release=prompt('Runtime release',currentRelease);if(!release)return;const artifactHash=prompt('CIA or 3DSX SHA-256 (64 lowercase hex characters)');if(!artifactHash)return;const consoleModel=prompt('Console: old-3ds, old-3ds-xl, new-3ds, new-3ds-xl, new-2ds-xl, emulator','old-3ds-xl');const installMethod=prompt('Install method: cia or 3dsx','cia');const transport=prompt('Transport: wss or tcp','wss');const result=prompt('Result: pass, partial, or fail','pass');const notes=prompt('Short testing notes with no credentials or private save data','')??'';try{await api('/api/community/compatibility-reports',{method:'POST',body:JSON.stringify({release,artifactHash:artifactHash.toLowerCase(),consoleModel,installMethod,transport,result,notes})});notice('Compatibility contribution recorded as Server-observed.');await openProfile()}catch(error){notice(error.message,true)}}
async function openLeaderboard(){if(!requirePairing())return;state.leaderboardPage=1;show('leaderboardview');await loadLeaderboard()}
async function loadLeaderboard(){if(!requirePairing())return;const board=$('boardselect').value,variant=board==='frontier-streak'?$('variant').value:'';const params=new URLSearchParams({board,variant,release:$('releaseversion').value,page:String(state.leaderboardPage)});try{const data=await api('/api/community/leaderboards?'+params),host=$('leaderboard');host.replaceChildren();const policy=document.createElement('div');policy.className='pairbox';policy.textContent=data.policy.integrity_label+' · '+data.policy.explanation;host.append(policy);if(!data.policy.enabled){const disabled=document.createElement('p');disabled.className='empty';disabled.textContent='This board is intentionally disabled pending physical peer-confirmation testing.';host.append(disabled);return}if(data.entries.length)host.append(table(['Rank','Player ID','Score','Integrity'],data.entries.map(row=>[String(row.rank),'ID '+row.fingerprint,String(row.score),data.policy.integrity_label])));else{const empty=document.createElement('p');empty.className='empty';empty.textContent=board==='frontier-streak'&&!variant?'Enter a facility variant such as tower:singles:50.':'No accepted entries for this release and board.';host.append(empty)}const nav=document.createElement('div');nav.className='actions';const previous=button('Previous',()=>{if(state.leaderboardPage>1){state.leaderboardPage--;loadLeaderboard()}}),next=button('Next',()=>{if(state.leaderboardPage<data.pages){state.leaderboardPage++;loadLeaderboard()}});previous.disabled=data.page<=1;next.disabled=data.page>=data.pages;const label=document.createElement('span');label.className='muted';label.textContent='Page '+data.page+' of '+data.pages;nav.append(previous,label,next);host.append(nav)}catch(error){notice(error.message,true)}}
async function startPairing(){try{const data=await api('/api/community/pairing/start',{method:'POST'});state.pairing=data;$('paircode').textContent=data.code;$('pairmessage').textContent='Waiting for approval on the 3DS…';$('pairbox').classList.remove('hidden');clearInterval(state.pairTimer);state.pairTimer=setInterval(consumePairing,1500)}catch(error){notice(error.message,true)}}
async function consumePairing(){if(!state.pairing)return;try{const data=await api('/api/community/pairing/consume',{method:'POST',body:JSON.stringify(state.pairing)});clearInterval(state.pairTimer);state.pairing=null;$('pairbox').classList.add('hidden');state.session={paired:true,fingerprint:data.fingerprint,csrfToken:data.csrfToken,moderator:false};renderSession();await loadAll();notice('Browser paired successfully.')}catch(error){if(error.status!==409){clearInterval(state.pairTimer);$('pairmessage').textContent=error.message}}}
function cancelPair(){clearInterval(state.pairTimer);state.pairing=null;$('pairbox').classList.add('hidden')}
async function logout(){try{await api('/api/community/session/logout',{method:'POST'});state.session=null;renderSession();await loadAll();notice('Browser session revoked.')}catch(error){notice(error.message,true)}}
async function loadAll(){const data=await api('/api/community/categories');state.categories=data.categories;renderCategories();await loadTopics()}
function renderCategories(){const host=$('categories');host.replaceChildren();for(const category of state.categories){const button=document.createElement('button');button.className='category'+(category.readable?'':' locked')+(state.category===category.slug?' active':'');button.textContent=(category.readable?'':'🔒 ')+category.name;button.title=category.description;button.onclick=()=>{if(!category.readable){notice('Pair a 3DS to read this board.',true);return}state.category=category.slug;state.page=1;document.querySelectorAll('.category').forEach(x=>x.classList.remove('active'));button.classList.add('active');loadTopics()};host.append(button)}const all=document.querySelector('[data-category=""]');all.classList.toggle('active',!state.category);all.onclick=()=>{state.category='';state.page=1;renderCategories();loadTopics()};const select=$('topiccategory');select.replaceChildren();for(const category of state.categories.filter(x=>x.readable&&x.visibility!=='moderator')){const option=document.createElement('option');option.value=category.slug;option.textContent=category.name;select.append(option)}}
async function loadTopics(){show('listing');const params=new URLSearchParams({page:String(state.page)});if(state.category)params.set('category',state.category);if(state.query)params.set('q',state.query);try{const data=await api('/api/community/topics?'+params);renderTopics(data.topics);$('page').textContent='Page '+data.page;$('previous').disabled=data.page<=1;$('next').disabled=data.topics.length<data.pageSize}catch(error){notice(error.message,true)}}
function officialLabel(topic){if(topic.official_release)return 'OFFICIAL RELEASE v'+topic.release_version;if(topic.official_known_issue)return 'OFFICIAL KNOWN ISSUE';if(topic.official_publication)return 'OFFICIAL '+String(topic.publication_kind||'GUIDE').toUpperCase();return 'ID '+(topic.author_fingerprint||'DELETED')}
function renderTopics(topics){const host=$('topics');host.replaceChildren();if(!topics.length){const empty=document.createElement('div');empty.className='empty';empty.textContent='No topics here yet.';host.append(empty);return}for(const topic of topics){const row=document.createElement('article');row.className='topic';const copy=document.createElement('div'),title=document.createElement('h3'),meta=document.createElement('div'),side=document.createElement('div');title.textContent=(topic.pinned?'📌 ':'')+topic.title;meta.className='meta';meta.textContent=topic.category_name+' · '+officialLabel(topic)+' · '+topic.reply_count+' replies · '+date(topic.updated_at);copy.append(title,meta);if(topic.official_release||topic.official_publication){const badge=document.createElement('span');badge.className='badge official';badge.textContent=topic.official_publication?String(topic.publication_kind||'GUIDE').toUpperCase():'OFFICIAL';side.append(badge)}if(topic.official_known_issue){const badge=document.createElement('span');badge.className='badge official';badge.textContent='KNOWN ISSUE';side.append(badge)}if(topic.defect_status){const badge=document.createElement('span');badge.className='badge '+topic.severity;badge.textContent=topic.severity+' · '+topic.defect_status;side.append(badge)}if(topic.locked)side.append(' 🔒');row.append(copy,side);row.onclick=()=>openTopic(topic.id);host.append(row)}}
async function openTopic(id){try{const data=await api('/api/community/topics/'+id);state.current=data.topic;renderTopic();show('topicview');history.replaceState(null,'','/community?topic='+id)}catch(error){notice(error.message,true)}}
async function openPublication(key){try{const data=await api('/api/community/publications/'+key);state.current=data.topic;renderTopic();show('topicview');history.replaceState(null,'','/community?guide='+key)}catch(error){notice(error.message,true)}}
function button(text,handler,kind='secondary'){const el=document.createElement('button');el.textContent=text;el.className=kind;el.onclick=handler;return el}
function renderTopic(){const t=state.current,host=$('topic');host.replaceChildren();const h=document.createElement('h1');h.textContent=t.title;const meta=document.createElement('p');meta.className='meta';meta.textContent=t.category_name+' · '+officialLabel(t)+' · '+date(t.created_at)+(t.locked?' · LOCKED':'');const body=document.createElement('div');body.className='body';body.innerHTML=t.body_html;host.append(h,meta);if(t.official_release){const official=document.createElement('div');official.className='pairbox';official.textContent='OFFICIAL RELEASE · Version '+t.release_version+' · published '+date(t.released_at)+(t.source_commit?' · source '+t.source_commit:'');host.append(official)}if(t.official_known_issue){const official=document.createElement('div');official.className='pairbox';official.textContent='OFFICIAL KNOWN ISSUE · Confirmed performance defect · workaround and evidence request below';host.append(official)}if(t.official_publication){const official=document.createElement('div');official.className='pairbox';official.textContent='OFFICIAL '+String(t.publication_kind||'guide').toUpperCase()+' · Maintained by the Emerald Online 3DS project';host.append(official)}if(t.defect_id){const defect=document.createElement('div');defect.className='pairbox';defect.textContent='Defect '+t.severity+' · '+t.defect_status+' · '+t.console_model+' · '+t.install_method.toUpperCase()+' · '+t.transport.toUpperCase()+' · runtime '+t.runtime_version;host.append(defect)}host.append(body);const actions=document.createElement('div');actions.className='actions';if(state.session){actions.append(button(t.subscribed?'Unsubscribe':'Subscribe',()=>subscribe(!t.subscribed)));actions.append(button('Report',()=>report({topicId:t.id})));if(t.author_fingerprint===state.session.fingerprint||state.session.moderator){actions.append(button('Edit',editTopic));actions.append(button('Delete',deleteTopic,'danger'))}if(state.session.moderator){actions.append(button(t.pinned?'Unpin':'Pin',()=>moderate({pinned:!t.pinned})));actions.append(button(t.locked?'Unlock':'Lock',()=>moderate({locked:!t.locked})));if(!t.official_release&&!t.official_known_issue&&!t.official_publication)actions.append(button('Sanction author',sanctionAuthor,'danger'));if(t.defect_id)actions.append(button('Update defect',updateDefect))}}host.append(actions);for(const post of t.posts||[]){const article=document.createElement('article');article.className='post';const by=document.createElement('p');by.className='meta';by.textContent='ID '+(post.author_fingerprint||'DELETED')+' · '+date(post.created_at);const content=document.createElement('div');content.className='body';content.innerHTML=post.body_html;if(state.session&&(post.author_fingerprint===state.session.fingerprint||state.session.moderator)){const edit=button('Edit',()=>editReply(post));const del=button('Delete',()=>deleteReply(post),'danger');const tools=document.createElement('div');tools.className='actions';tools.append(edit,del);article.append(by,content,tools)}else article.append(by,content);if(state.session)article.append(button('Report',()=>report({postId:post.id})));host.append(article)}if(state.session&&!t.locked){const form=document.createElement('form');form.className='composer';const area=document.createElement('textarea');area.maxLength=10000;area.required=true;area.placeholder='Write a reply…';const submit=document.createElement('button');submit.textContent='Reply';form.append(area,submit);form.onsubmit=async event=>{event.preventDefault();try{await api('/api/community/topics/'+t.id+'/replies',{method:'POST',body:JSON.stringify({body:area.value})});await openTopic(t.id)}catch(error){notice(error.message,true)}};host.append(form)}}
async function subscribe(on){await api('/api/community/topics/'+state.current.id+'/subscription',{method:'PUT',body:JSON.stringify({subscribed:on})});await openTopic(state.current.id)}
async function report(target){const reason=prompt('Why should moderators review this?');if(!reason)return;try{await api('/api/community/reports',{method:'POST',body:JSON.stringify({...target,reason})});notice('Report submitted privately to moderators.')}catch(error){notice(error.message,true)}}
async function editTopic(){const title=prompt('Title',state.current.title),body=prompt('Markdown body',state.current.body_markdown);if(!title||!body)return;await api('/api/community/topics/'+state.current.id,{method:'PATCH',body:JSON.stringify({title,body})});await openTopic(state.current.id)}
async function deleteTopic(){if(!confirm('Delete this topic? It will remain recoverable to moderators for 30 days.'))return;await api('/api/community/topics/'+state.current.id,{method:'DELETE'});history.replaceState(null,'','/community');await loadTopics()}
async function editReply(post){const body=prompt('Markdown reply',post.body_markdown);if(!body)return;await api('/api/community/posts/'+post.id,{method:'PATCH',body:JSON.stringify({body})});await openTopic(state.current.id)}
async function deleteReply(post){if(!confirm('Delete this reply?'))return;await api('/api/community/posts/'+post.id,{method:'DELETE'});await openTopic(state.current.id)}
async function moderate(changes){await api('/api/community/moderation/topics/'+state.current.id,{method:'PATCH',body:JSON.stringify(changes)});await openTopic(state.current.id)}
async function updateDefect(){const status=prompt('Status: new, needs-information, confirmed, in-progress, needs-retest, fixed, closed',state.current.defect_status);if(!status)return;await api('/api/community/moderation/defects/'+state.current.defect_id,{method:'PATCH',body:JSON.stringify({status})});await openTopic(state.current.id)}
async function sanctionAuthor(){const kind=prompt('Sanction: warning, read-only, or suspended','read-only');if(!kind)return;const reason=prompt('Private moderation reason');if(!reason)return;await api('/api/community/moderation/sanctions',{method:'POST',body:JSON.stringify({topicId:state.current.id,kind,reason})});notice('Sanction recorded in the moderation audit.')}
async function openModeration(){try{const data=await api('/api/community/moderation/overview'),stats=await api('/api/community/moderation/stats');const host=$('topic');host.replaceChildren();const title=document.createElement('h1');title.textContent='Moderation';host.append(title);const section=(name,items,render)=>{const heading=document.createElement('h2');heading.textContent=name;host.append(heading);if(!items.length){const empty=document.createElement('p');empty.className='muted';empty.textContent='None';host.append(empty)}for(const item of items)host.append(render(item))};section('Leaderboard anomaly review',stats.entries,item=>{const row=document.createElement('div');row.className='post';row.textContent='ID '+item.fingerprint+' · '+item.board+' '+(item.variant||'')+' · '+item.score+' · '+item.review_reason;row.append(button('Accept',async()=>{await api('/api/community/moderation/stats/'+item.id,{method:'PATCH',body:JSON.stringify({decision:'accept'})});openModeration()}),button('Dismiss claim',async()=>{await api('/api/community/moderation/stats/'+item.id,{method:'PATCH',body:JSON.stringify({decision:'dismiss'})});openModeration()},'danger'));return row});section('Reports',data.reports,item=>{const row=document.createElement('div');row.className='post';row.textContent=item.status+' · '+item.reason+' · '+date(item.created_at);if(!['resolved','dismissed'].includes(item.status))row.append(button('Resolve',async()=>{await api('/api/community/moderation/reports/'+item.id,{method:'PATCH',body:JSON.stringify({status:'resolved'})});openModeration()}));return row});section('Sanctions',data.sanctions,item=>{const row=document.createElement('div');row.className='post';row.textContent='ID '+item.fingerprint+' · '+item.kind+' · '+item.reason;if(!item.revoked_at)row.append(button('Revoke',async()=>{await api('/api/community/moderation/sanctions/'+item.id,{method:'DELETE'});openModeration()}));return row});section('Recoverable topics',data.deletedTopics,item=>{const row=document.createElement('div');row.className='post';row.textContent=item.title+' · purge '+date(item.purge_after);row.append(button('Restore',async()=>{await api('/api/community/moderation/topics/'+item.id+'/restore',{method:'POST'});openModeration()}));return row});section('Recoverable replies',data.deletedPosts,item=>{const row=document.createElement('div');row.className='post';row.textContent='Reply '+item.id+' · purge '+date(item.purge_after);row.append(button('Restore',async()=>{await api('/api/community/moderation/posts/'+item.id+'/restore',{method:'POST'});openModeration()}));return row});section('Audit history',data.audit,item=>{const row=document.createElement('div');row.className='post';row.textContent=item.action+' · '+date(item.created_at);return row});show('topicview')}catch(error){notice(error.message,true)}}
function compose(){if(!state.session){startPairing();return}show('composer');$('topiccategory').value=state.category&&state.category!=='bugs-defects'?state.category:'general';toggleDefect()}
function toggleDefect(){$('defectfields').classList.toggle('hidden',$('topiccategory').value!=='bugs-defects')}
async function submitTopic(event){event.preventDefault();const category=$('topiccategory').value,payload={category,title:$('topictitle').value,body:$('topicbody').value};if(category==='bugs-defects')payload.defect={severity:$('severity').value,runtimeVersion:$('runtimeversion').value,consoleModel:$('consolemodel').value,installMethod:$('installmethod').value,transport:$('transport').value,artifactHash:$('artifacthash').value,reproductionSteps:$('reproduction').value,expectedBehavior:$('expected').value,actualBehavior:$('actual').value,diagnosticText:$('diagnostics').value};try{const created=await api('/api/community/topics',{method:'POST',body:JSON.stringify(payload)});event.target.reset();await loadAll();await openTopic(created.id)}catch(error){notice(error.message,true)}}
$('cancelpair').onclick=cancelPair;$('profilebutton').onclick=openProfile;$('leaderboardbutton').onclick=openLeaderboard;$('loadboard').onclick=()=>{state.leaderboardPage=1;loadLeaderboard()};document.querySelectorAll('.returntopics').forEach(element=>element.onclick=()=>loadTopics());$('searchbutton').onclick=()=>{state.query=$('search').value;state.page=1;loadTopics()};$('search').onkeydown=e=>{if(e.key==='Enter')$('searchbutton').click()};$('newtopic').onclick=compose;$('back').onclick=()=>{history.replaceState(null,'','/community');loadTopics()};$('cancelcompose').onclick=()=>loadTopics();$('topiccategory').onchange=toggleDefect;$('topicform').onsubmit=submitTopic;$('previous').onclick=()=>{if(state.page>1){state.page--;loadTopics()}};$('next').onclick=()=>{state.page++;loadTopics()};
(async()=>{try{const params=new URL(location.href).searchParams,requestedCategory=params.get('category');if(requestedCategory&&/^[a-z0-9-]{2,40}$/.test(requestedCategory))state.category=requestedCategory;await loadSession();await loadAll();const id=params.get('topic'),guide=params.get('guide');if(id)await openTopic(id);else if(guide&&/^[a-z0-9][a-z0-9-]{2,79}$/.test(guide))await openPublication(guide)}catch(error){notice('Community service unavailable: '+error.message,true)}})();
})();`;
